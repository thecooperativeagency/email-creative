# Ada — private archive

Private slideshow for Lance + Ada only.
Do not link from hub, client sites, or public Pages.

Local: `python3 -m http.server 8765 --bind 127.0.0.1` then http://127.0.0.1:8765/
